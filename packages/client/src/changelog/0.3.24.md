2026-08-10
#### Hotfix ####
- **Mulligan panel is now consistent across screen types** — it was sized in `vh` (relative to the real browser window), so on tall/large screens it grew past the board and tucked up under the turn bar. It's now sized in the game's fixed design canvas, so it looks and sits identically on every monitor.
- **Draggable panels can no longer be dragged up over the turn bar** — prompt banners, the action menu, prompt dialogs, the editor panel, and the mulligan all stop at the top bar when shoved aside.
