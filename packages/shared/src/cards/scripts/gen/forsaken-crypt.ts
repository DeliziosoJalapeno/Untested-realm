import { registerScript } from '../registry'
import { collectionSiteScript } from '../multi-card-utils/collection-site'

registerScript('Forsaken Crypt', collectionSiteScript('Forsaken Crypt', 'Ghoul', 'air', 2))
