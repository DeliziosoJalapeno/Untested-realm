import { registerScript } from '../registry'

// 'Bearer's controller has "Whenever you would determine an outcome at random,
// determine it an extra time and choose one."' (see isLucky)
registerScript('Lucky Charm', {})
