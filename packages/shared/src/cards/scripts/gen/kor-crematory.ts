import { registerScript } from '../registry'

// '(F)(F)(F)(F) — If a card would be placed in a cemetery, banish it instead.'
// (enforced centrally in effects.ts toCemetery)
registerScript('Kor Crematory', {})
