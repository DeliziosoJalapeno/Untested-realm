import { registerScript } from '../registry'

// "Bearer has Burrowing, Submerge, and Voidwalk, if it's a minion."
registerScript('All-terrain Vestments', { bearerKeywords: ['burrowing', 'submerge', 'voidwalk'] })
